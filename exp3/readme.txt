代码主要架构：
1.初始化UI界面，初始化plot界面，当点击按钮时执行callback()函数，读取输入的RLC值，当输入的RLC值符合要求（即为数字,用is_number()函数判断）时，
  执行fun_show()函数
2.定义fun_show()函数，将读入的值带入公式中运算得到阻尼系数a和谐振频率w0，通过a和w0的大小判断电路状态（无阻尼、过阻尼、临界阻尼），
  不同的电路状态代入不同的公式中计算得到 iL (t)、vc(t)；最后将状态轨迹图绘制出来